cubes:
  - name: Credit_by_Scheduled
    joins: []
    dimensions:
      - name: region
        sql: region
        type: string
      - name: state_name
        sql: state_name
        type: string
      - name: state_code
        sql: state_code
        type: string
      - name: district_name
        sql: district_name
        type: string
      - name: district_code
        sql: district_code
        type: string
      - name: population_group
        sql: population_group
        type: string
      - name: bank_group
        sql: bank_group
        type: string
      - name: occupation_group
        sql: occupation_group
        type: string
      - name: occupation_subgroup
        sql: occupation_subgroup
        type: string
      - name: year
        sql: year
        type: time
    measures:
      - name: no_of_accounts
        sql: SUM(no_of_accounts)
        type: number
      - name: credit_limit
        sql: SUM(credit_limit)
        type: number
      - name: amount_outstanding
        sql: SUM(amount_outstanding)
        type: number
    sql_table: "public.\"1e666821-8c49-4000-89ac-2a33659565e3\""
